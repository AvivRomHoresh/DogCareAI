import { PageCard } from '../components/PageCard';

export function DogProfilePage() {
  return (
    <PageCard
      title="Dog Profile route stub"
      description="The dog profile form, static avatar picker, Supabase persistence, and validation are intentionally deferred."
    />
  );
}
