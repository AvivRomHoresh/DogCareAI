import { PageCard } from '../components/PageCard';

export function RemindersPage() {
  return (
    <PageCard
      title="Reminders route stub"
      description="Reminder creation, completion, editing, and Supabase persistence are intentionally deferred."
    />
  );
}
